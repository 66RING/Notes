---
title: 机器学习与数据分析
date: 2019-9-8
tags: machine learning
---

### Numpy

#### 打开数据：numpy.genfromtxt()

```python
numpy.genfromtxt(PATH,delimiter="分隔符",dtype="类型")
```

#### 最核心：numpy.array()

```python
# 把数据变成数组->类似matlab的矩阵
vector = numpy.array()
# numpy.array()会把里面的东西换成一样的类型

1.vector.shape
# 返回vectoe的规模，几维数组

2.vector==10
# 相当于把vector里面的每个元素都进行==10的判断
# 返回boolean列表

3.vector['判断方法']
equal_to_ten = (vector == 10) # equal_to_ten就是一个bool的列表
vector[equal_to_ten] # 按equal_to_ten的列表顺序，返回值

4.vector.astype(TYPE) 
# 整体类型转换

5.vector.min() && vector.max() && vector.sum()
# vector.max()里面提供参数可以指定sum的方法

6.vector.reshape(3,5)
# 变成个3*5的矩阵

7.numpy.zeros(规模)
# 初始化	全为0的矩阵

8. numpy.ones((规模),dtype=)
# 初始化一个，里面元素的类型自定

9.numpy.arange(start,end,step)

*10.numpy.random.random(规模)
# 正一到负一的值

*11.numpy.linespace(start,end,分多少份)
# 

12. numpy运算
# * 对应位置的乘
# A.dot(B) numpy.dot(A.B) A和B矩阵乘

13.numpy.floor()
# 向下取整

13.vector.ravel()
# 把矩阵拉成向量

14.矩阵拼接
# numpy.hstack() 横着拼接
# numpy.vstack() 竖着拼接

15.切分
# numpy.hsplit(a,3) 按行切分
# numpy.vsplit() 按列切分
# 把a切成3份
# 3的位置可以传入一个元组，就会在元组的位置切分

16.


```



